var x, y = "breno", z = (8 + k)
const a = 7, b = "breno"
var c = "compiler", d = (3+x*3)

procedure varrer()
	function fatorial(x)
	begin

	end
begin

end

function delta(a, b, c)
	var del, raiz
begin
	del = b*b + 4*a*c;
	imprimir(del);
	raiz = sqrt(del)
end
